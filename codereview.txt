Yes. I went through the codebase rather than just eyeballing the first script. My overall reaction is: this is substantially better code than I expected from a personal utility of this size. It has clearly evolved from actual failures rather than hypothetical architecture. There are some real problems, though, including a couple I would fix immediately.Overall grade: B+ / A- for a personal engineering toolIf I were reviewing this as production software maintained by a team, I'd bring that down to about a B because of portability, test coverage, stale documentation, global state, and overall complexity. But for a specialized tool built to solve a nasty real-world STL-processing problem on your own machine, it's quite strong.The most impressive part isn't whether every individual function is beautiful. It's that the code shows evidence of observing failures, determining why they happened, and redesigning around them. The multiprocessing sections in particular contain lessons that usually only get written after somebody has spent hours watching Linux processes do horrible shit.I found these concrete issues:The direct CLI is broken with the current default WORKERS = 0. Near the bottom of stl_batch_fix.py you do:workers = min(WORKERS, len(files))With the current default, that becomes 0, followed by:ProcessPoolExecutor(max_workers=workers, ...)max_workers=0 is invalid.The TUI correctly converts WORKERS=0 using auto_worker_count(), but the standalone script doesn't. And your run.sh documentation explicitly tells people they can invoke the underlying script directly.That is a genuine bug, not style criticism. I'd change it to essentially:sized = measure_files(files)workers = auto_worker_count(sized) if WORKERS <= 0 else WORKERSworkers = min(workers, len(files))BLENDER_BIN is advertised but doesn't actually control Blender. run.sh says:BLENDER_BIN=/path/to/blenderand install.sh honors it.But stl_batch_fix.py has:BLENDER = "blender"and never reads BLENDER_BIN.The TUI then tests:_fix.shutil.which(_fix.BLENDER)So someone can set BLENDER_BIN=/some/custom/blender, have the installer successfully detect it, then have the actual program say Blender isn't installed.I'd simply make this:BLENDER = os.environ.get("BLENDER_BIN", "blender")That's another real bug.Your documentation has diverged significantly from the implementation. This jumped out at me because the code comments themselves are considerably more current than STL_BATCH_FIX_DESIGN.md.For example, the design doc says the default is:WORKERS = 3The code says:WORKERS = 0The document says .part.N.stl files are collected and specifically says:“Do not exclude .part.N.stl from collect_stl_files”The current implementation explicitly does exactly that:if _PART_MARKER in name_lower:    return FalseThat's probably because your architecture changed—you now process a split file and its parts inside one worker. That's perfectly reasonable. But it makes the design document actively misleading.This isn't cosmetic. With software this complicated, stale architecture documentation can cause a future fix to reintroduce an old bug because somebody follows the document instead of the code.There are no automated tests. This is the largest structural weakness.I couldn't find a test suite at all.And this project is now large enough that tests would pay for themselves. You have a bunch of excellent pure-ish functions that are easy to test without Blender:output_path()collect_stl_files()partition_already_done()_read_stl_header()check_stl_integrity()is_ascii_stl()estimate_peak_bytes()mesh_is_too_large()auto_worker_count()plan_worker_count()rotate_log()You don't need to build some gigantic mock-Blender monstrosity. Twenty or thirty tests around those pieces would give you disproportionate protection.And, amusingly, a simple test of:WORKERS = 0against standalone execution probably would have immediately caught issue #1.There is too much module-level mutable configuration.You have globals like:INPUT_FOLDEROUTPUT_SUFFIXMERGE_DISTWORKERSTIMEOUTMAX_FACESLOG_FILEREVIEW_FILEand then the TUI does:_fix.INPUT_FOLDER  = values['INPUT_FOLDER']_fix.OUTPUT_SUFFIX = values['OUTPUT_SUFFIX']...It works largely because you're consciously relying on fork, and you've documented that dependency. But it makes the system harder to reason about and harder to test.I wouldn't rewrite the whole thing. That's exactly the kind of "architectural cleanup" that can turn functioning software into a six-week science project.But if you ever do a V2, I'd introduce something like:@dataclassclass FixConfig:    input_folder: str    output_suffix: str    merge_dist: float    workers: int    timeout: int    max_faces: int    recursive: boolThen hand that around explicitly.One thing I particularly likeYour comments are unusually useful.For example, this isn't a useless comment:ProcessPoolExecutor fails EVERY pending future when one of its own workers dies...You explain the actual production failure, why the previous architecture failed, and why the subprocess boundary exists.Likewise, the explanation of why RLIMIT_AS was removed is very good. You're distinguishing virtual address space from RSS and explaining the observed std::bad_alloc behavior rather than merely saying:"Don't use RLIMIT."Same thing with the Manager proxy timeout, zombie detection through /proc, memory scheduling, and process-pool abandonment. Those comments preserve engineering knowledge, not just restate the Python.That's one of the strongest parts of this code.The multiprocessing code is sophisticated — but it's also your danger zoneYour process management is simultaneously some of the best code here and the part I'd be most nervous changing.You've dealt with:dead workersorphan Blender processespool poisoningbroken pipesstuck multiprocessing queuesManager hangsSIGTERM/SIGKILL escalationzombiesOOM conditionsfiles lost when another worker crasheschild-process result isolationThat's serious machinery.The fact that you have things like:pool._processesis technically using a CPython implementation detail. Normally I'd slap somebody's wrist for doing that.Here I wouldn't.You're using it because the public API doesn't provide sufficient control for the failure mode you're handling. The comment explains why. That's an acceptable ugly hack.I would just isolate every such dependency behind functions, which you've mostly done, so a Python upgrade that changes internals has a small blast radius.Your memory-control logic is thoughtfulThis part is much better than the usual:workers = os.cpu_count()and pray.You've modeled estimated memory per triangle:_BYTES_PER_TRIANGLE = 5.8 * 1024**3 / 7_000_034and then use that for both:mesh_is_too_large()and:plan_worker_count()You're also distinguishing:machine RAMcurrently available RAMwhole-job cgroup limitper-mesh projected requirementsconcurrencyThat's sensible.There is, however, a comment/code mismatch in auto_worker_count().The docstring says:“The 90th percentile is used instead”but the actual code says:ref = sizes[len(sizes) // 2]which is the median.Then the nearby implementation comment explains that you deliberately switched to median because p90 was overly conservative.So the code probably represents the correct latest decision, but the docstring wasn't updated. Another good example of documentation drift.I like your failure-state designUsing actual STL copies as:.failed.stl.timeout.stl.unrepaired.stl.broken.stl.open.stlis clever.It makes state visible in the filesystem, preserves the geometry involved, and allows a human to open the "marker" directly in a mesh viewer.That is much more useful than .failed zero-byte semaphore files.Your cleanup/retry logic also clearly recognizes that crashes don't always allow finally: to run. That's a very practical design decision.I would change the hardcoded pathsThis:#!/mnt/sda2/python/.venv/bin/pythonshould go.You already have run.sh deciding which Python to use, and the TUI uses:#!/usr/bin/env python3The main script should probably do the same.Likewise:LOG_FILE = "/mnt/sda2/STL/Fixed/repair_log.tsv"REVIEW_FILE = "/mnt/sda2/STL/Fixed/review_decimated.tsv"ties internal behavior to your particular filesystem even if somebody changes INPUT_FOLDER.Your output path is dynamically based on INPUT_FOLDER, so I'd derive logs from the same output root.Right now someone can run:python stl_batch_fix.py --input /home/bob/modelsand the repaired meshes go under /home/bob/Fixed, while the logs still go to /mnt/sda2/STL/Fixed.That's surprising.There is some accumulated scar tissueAt 2,624 lines in the core and 1,334 lines in the TUI, this thing is beginning to cross the point where one module should probably become several.I would not embark on a full rewrite.But natural boundaries are already visible:stl_io.py    STL parsing/scanning/bounds/integrity
repair.py    PyMeshLab/PyMeshFix/Blender pipeline
workers.py    process isolation / signals / memory control
files.py    collection/output/state/logging
config.py    configuration
tui.py    presentationThat would bring stl_batch_fix.py back into the hundreds of lines rather than thousands.Again: don't refactor it merely to satisfy someone's abstract notion of clean architecture. Do it incrementally when you're touching those areas anyway.Some broad except Exceptions are justified; some could be narrowedI found quite a few of them.Normally that would concern me. After reading the surrounding code, a surprising number make sense because you're deliberately insulating the controller from flaky native libraries and cleanup failures.For example:try:    ...except Exception:    passinside teardown code is often exactly right. A cleanup failure shouldn't mask the original result.But I'd review the ones in actual mesh-processing logic individually. Native-library failure should be captured, but ideally turned into something structured like:except Exception as exc:    return RepairFailure(stage="pymeshfix", error=str(exc))rather than disappearing.You're already doing this in a number of places.The one subtle input concern I'd testis_ascii_stl() determines ASCII by looking at only the first 256 bytes:header = f.read(256)looks_ascii = (    header.lstrip()[:5].lower() == b'solid'    and b'facet normal' in header.lower())That's probably good for virtually every STL you'll encounter, and you correctly handle binary STLs whose header begins with solid.But a valid ASCII STL with an unusually long header/name before its first facet could be classified as binary.Not something I'd urgently rewrite. I'd just create tests covering ugly STL headers so you know where the boundaries are.So the main priority order I'd use is:Fix standalone WORKERS=0.Actually honor BLENDER_BIN.Make log paths derive from the selected input/output location.Update STL_BATCH_FIX_DESIGN.md because it now describes an older architecture.Add a small pytest suite around file/config/memory/state behavior.Then, and only then, consider gradually separating that 2,600-line module.The important part is that I would not rewrite the repair pipeline simply to make it prettier. There is a shitload of hard-earned behavior embedded in this code. A clever developer could easily turn this into a gorgeous 800-line rewrite that promptly OOMs, strands Blender processes, loses 40 queued files when one worker dies, and starts mangling your models.This code has scars for reasons.And after reading those scars, I trust it considerably more than I would trust a superficially cleaner implementation that hadn't actually survived your STL collection.