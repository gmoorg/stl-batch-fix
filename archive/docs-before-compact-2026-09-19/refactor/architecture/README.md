# Target — the module design

Read [the original target](overview.md) for intent, then [current state](../current-state.md) for the implementation. D15 changed the proposed no-geometry DTO into a `Mesh` with geometry loaded explicitly.
